# photon-runtime

`Photon`, `PhotonBuilder`, and process-wide configuration.

## Exports

- `Photon`, `PhotonBuilder`, `PhotonRuntimeState`
- `configure`, `default`
- `runtime::build_photon_parts`

## Status

Shipped. Depends on [`photon-backend`](../photon-backend/).
